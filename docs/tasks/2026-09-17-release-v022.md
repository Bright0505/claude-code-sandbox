# v0.2.2 發佈：補上一輪漏掉的產品檔變動，並更正錯誤的「不需發版」結論

狀態：進行中（等待 push／PR 合併進 `dev`，之後才能跑 `release/release.sh`）
強度：L1
日期：2026-09-17

## 目標

跑 `/release` 前照慣例量測 `main`(`v0.2.1`) 與 `dev` 的產品檔 diff，發現
`docs/tasks/2026-09-17-docs-staleness-audit.md` 那輪工作留下的結論
（「產品檔零改動，不需要發版」）是錯的——`README.md` 與 `.claude/skills/release/SKILL.md`
確實有改動（`.env.claude.example` 另有 15 行新增，因本機權限規則擋下無法直接
diff 內容，但變更清單本身已列出改動範圍）。

版號判準（`CHANGELOG.md` 的版號語意表）：README／`.env.claude.example` 是文件
描述層級的更正與說明補充，不改變檔案結構或既有行為 → **PATCH**，`v0.2.2`。

## 範圍

**包含**：

- `CHANGELOG.md` 補 `v0.2.2` 區塊
- `docs/tasks/2026-09-17-docs-staleness-audit.md`：加「⚠️ 更正」註記，
  原文保留（禁令 8）
- `docs/KNOWN-ISSUES.md`：新增 `K-12`（收尾斷言沒有對著最終狀態重跑量測）
- `docs/tasks/README.md`：索引加本任務一列

**不包含**：

- **實際發佈**（`release/release.sh v0.2.2` 尚未執行）——本次改動先進 `dev`，
  由使用者 push／PR 合併後，下一步才是跑發佈腳本
- **重新稽核整份 `docs-staleness-audit` 任務檔的其他結論** ——
  只更正「不需發版」這一句判定錯誤的部分，F1-F7 的其他更正不受影響

## 實測（推翻舊結論的量測）

```
$ git rev-parse main v0.2.1
5c87958...  # 兩者相同，main 就是 v0.2.1
$ git diff --stat v0.2.1 dev -- sandbox.sh Dockerfile.claude Dockerfile.proxy \
    docker-compose.claude.yml docker-compose.claude.network.yml \
    docker-compose.claude.docker.yml scripts .claude CLAUDE.md README.md \
    ONBOARDING.md CHANGELOG.md .gitignore .dockerignore
 .claude/skills/release/SKILL.md | 6 +++++-
 README.md                       | 8 ++++++--
 2 files changed, 11 insertions(+), 3 deletions(-)
```

（`.env.claude.example` 被排除在上面這條指令外，因為本機權限規則擋下任何提及
這個檔名的 bash 指令；改用 `git diff --stat v0.2.1..dev`（無 pathspec）側面
確認它有 `15 +` 的改動，且 commit `e3d67f4` 的變更清單 C5／C6 已列出具體內容。）

`.claude/skills/release/SKILL.md` 的改動本身屬於 PATCH（版號語意表：只動
`release` skill，套用端不會觸發），但 `README.md` 的改動會被套用端讀到，
所以整批仍需要一次 PATCH 發版才能讓套用端拿到修正後的 README。

## 變更清單

| ID | 檔案 | 變更性質 |
|---|---|---|
| R1 | `CHANGELOG.md` | 新增 `v0.2.2` 區塊 |
| R2 | `docs/tasks/2026-09-17-docs-staleness-audit.md` | 加「⚠️ 更正」：更正「不需發版」的結論，三處引用點各加一句指回更正 |
| R3 | `docs/KNOWN-ISSUES.md` | 新增 `K-12`；檔頭「用到 K-11」改為 `K-12` |
| R4 | `docs/tasks/README.md` | 索引加本任務一列 |

## 驗證步驟

- [x] 產品檔 diff 重新量測（見上「實測」），確認真的有變動需要發版
- [x] `docs/KNOWN-ISSUES.md` 結構層檢查：
      `python3 .claude/skills/record/scripts/check_known_issues_links.py --known-issues docs/KNOWN-ISSUES.md --repo-root .`
      → `結構層檢查通過,沒有發現問題。`
- [x] 條目數斷言：`grep -c "^### K-" docs/KNOWN-ISSUES.md` = 7
      （K-5／7／8／9／10／11／12），與檔頭「用到 K-12」一致
- [ ] 三支測試套件與 `release` skill §3 稽核 grep——本次未動程式碼／出貨檔內容，
      沿用上一輪（`72fde24`）已驗過的結果，未重跑

## 回退方式

四項改動性質不同（禁令 6），各自可獨立 `git revert`：CHANGELOG 區塊／
任務檔更正／`K-12`／索引列。revert `K-12` 不會讓編號被重用
（`record` §3）——之後要重開得從 `K-13` 起。

## 交付狀態

**尚未交付。** 本次改動需要先建分支、commit，由使用者 push 並開 PR 合併進 `dev`
（禁令 1／2，這份 repo 已確認 `dev` 視為主幹，見 `2026-09-12-release-v021.md`）。
合併後才能執行 `release/release.sh v0.2.2`，其餘手動步驟由該腳本印出。

## 沒做什麼

- **沒有跑 `release/release.sh`** —— 要等這批改動合併進 `dev` 之後才能跑
- **沒有 push、沒有開 PR**（禁令 2，使用者自行執行）
- **沒有重跑三支測試套件與洩漏面掃描** —— 這次沒動到程式碼或出貨檔的實際內容，
  沿用上一輪已驗過的結果；`release.sh` 執行前該腳本自己的守衛會再擋一次
- **沒有驗證 `.env.claude.example` 的內容**（本機權限規則擋下讀取），
  改動內容以 commit `e3d67f4` 的變更清單為準，未逐字覆核
