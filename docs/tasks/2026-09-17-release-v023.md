# v0.2.3 發佈：release skill 補 GitHub Release 步驟

狀態：進行中（等待 push／PR 合併進 `dev`，之後才能跑 `release/release.sh`）
強度：L1
日期：2026-09-17

## 目標

發佈 `v0.2.3`：把上一輪加進 `release/release.sh` 與 `release` skill 的
「建 GitHub Release，內容從 CHANGELOG 擷取」步驟正式出貨（見
`2026-09-17-release-notes-from-changelog.md`）。

版號判準（`CHANGELOG.md` 的版號語意表）：這次唯一出貨的變動是
`.claude/skills/release/SKILL.md`，內容只影響上游自己的發佈流程，
套用端不會觸發 → **PATCH**（語意表明講的例子）。

## 範圍

**包含**：`CHANGELOG.md` 補 `v0.2.3` 區塊。

**不包含**：

- `release/release.sh` 本身的改動——它不出貨（`NOT_SHIPPED`），不需要
  CHANGELOG 條目，只是這次出貨的 `SKILL.md` 內容是它的說明文件

## 實測（產品檔 diff）

```
$ git diff --stat main dev -- sandbox.sh Dockerfile.claude Dockerfile.proxy \
    docker-compose.claude.yml docker-compose.claude.network.yml \
    docker-compose.claude.docker.yml scripts .claude CLAUDE.md README.md \
    ONBOARDING.md CHANGELOG.md .gitignore .dockerignore
 .claude/skills/release/SKILL.md | 27 ++++++++++++++++++++++++++-
 1 file changed, 26 insertions(+), 1 deletion(-)
```

只有一個檔案，跟這次要出貨的內容一致（吸取上一輪的教訓：這次在寫 CHANGELOG
之前就先重跑過這條量測，不是複述先前的印象——見 `K-12`）。

## 驗證步驟

- [x] 產品檔 diff 重新量測（見上），確認只有 `SKILL.md` 一個檔案
- [ ] `release/release.sh v0.2.3` 的四道守衛（待 `dev` 合併後執行）
- [ ] §9 產物端驗收（待發佈後執行）

## 交付狀態

**尚未交付。** 需要先 push 分支、開 PR 合併進 `dev`，合併後才能跑
`release/release.sh v0.2.3`。

## 沒做什麼

- **沒有跑 `release/release.sh`** —— 要等這個 commit 合併進 `dev` 之後
- **沒有 push、沒有開 PR**（禁令 2，使用者自行執行或明確指示才做）
