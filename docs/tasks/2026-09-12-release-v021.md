# v0.2.1 發佈：TERM 透傳，以及稽核抓到的一個跨版死指標

狀態：**進行中（卡在人的步驟）**
強度：L2
日期：2026-09-12

## 目標

發佈 `v0.2.1`：sandbox 容器帶入 host 的 `TERM`／`COLORTERM`。

版號判準是「套用端要不要動手」（`CHANGELOG.md` 的版號語意表）：這次只動
`docker-compose.claude.yml` 的 `environment`，不用重建映像檔 → **PATCH**。

## 範圍

**包含**：`docker-compose.claude.yml`、`CHANGELOG.md` 的 v0.2.1 區塊、
`scripts/test-git-auth.sh` 的檔頭註解（§3 稽核抓到的）。

**不包含**：

- **沒有復現「複製不到畫面文字」這個症狀本身。** 只驗到兩個變數確實進到容器
  （刻意用 `--no-TTY` 跑，排除 docker 配 TTY 時自動給 `TERM` 的干擾）。
  修掉的是一個必要條件，不保證是充分解 —— 已寫進 CHANGELOG 的「已知未驗」
- `scripts/test-git-auth.sh` 只做了 `bash -n`。整套 suite 是 Linux-only
  （見 v0.1.0 的 CHANGELOG），這台 macOS host 跑不完；這次的改動是純註解
- Linux host 上的驗證 —— 一直沒有那個環境（CHANGELOG 檔尾的固定小節）

## 稽核結果（`release` skill §3／§5）

### ① §3 引用審計：一個跨了三個版本的死指標

`scripts/test-git-auth.sh:6` 指向 `docs/tasks/2026-08-13-git-forge-token-auth.md`，
而那份任務檔在 `2d24251`（移除上游研發紀錄）就從 `dev` 拿掉了。
`scripts/` 是產品檔 → **v0.1.0 起每一版出貨的都帶著這個指不到東西的指標**。

為什麼前兩版的稽核沒抓到：v0.2.0 那次把範圍從「只掃 `.md`」擴到產品檔清單
（`K-5` 的教訓），但 pattern 只針對條目 ID `K-<n>`／`D<n>` 與 `docs/` 三個檔名，
**沒有涵蓋 `docs/tasks/<具體檔名>`**。這次補掃 `docs/tasks/[0-9]{4}-...` 才看到。

處理：不是把指標刪掉了事。被指向的「這套測不到什麼」在產品裡沒有別的載體
（§2：移除引用前先確認判準有載體），所以把那三項搬進腳本自己的檔頭；
自架 GitLab 那一項 `README.md:102-103` 已經有了，只留一行指過去。

### ② §5 洩漏面掃描：零命中，但零命中不算證據

11 條 pattern 跑一次出命中表。有命中的都能歸類、都不是洩漏：
`example.com` 佔位、`ghp_xxxxxxxx`／`glpat-xxxxxxxx` 佔位、
`172.31.x` 是 `test-init-firewall.sh` 裡假造的 `ip addr` 輸出、
`Bright0505/claude-code-sandbox` 是上游自己的位址（§7 要求取回指針要帶）。

第二角度（§5 要求）：把 `release/skeleton/` 三份整份讀過。零條目、
沒有上游專案名／網段／主機路徑。

### ③ 一次性設定

`gh repo view`：`isTemplate=true`、default branch `main`。兩項都對。

## 已發現但這次不處理

- **空骨架沒有自帶「上游原本的內容怎麼取回」**（`release` skill §1 要求兩樣，
  骨架只有第一樣）。取回方式在 `CHANGELOG.md` 檔尾的固定小節，載體存在，
  但骨架本身不指過去。v0.1.0／v0.2.0 都是這個形態，不在這次委託範圍內
- `release` skill §3 的示範指令（SKILL.md:98-101）也沒有涵蓋
  `docs/tasks/<具體檔名>` —— 就是上面 ① 漏掉的那個 pattern。
  修它等於改規則，另案

## 交付狀態

**這次不在 `dev` 上直接 commit**（上一輪確認過 `dev` 視為禁令 1 的主幹）。
兩支都以 `f96053d` 為 base，是兄弟關係：

| 分支 | 內容 |
|---|---|
| `docs/changelog-v021` | CHANGELOG v0.2.1 區塊 ＋ 這份任務檔 |
| `fix/test-git-auth-dangling-ref` | 上面 ① 的修正 |

**下一步（人做）**：兩支各推上去開 PR、合進 `dev`、`git pull`，
然後 `release/release.sh v0.2.1`，剩下的步驟腳本會印出來。

**push／PR 由使用者自己執行** —— 本 session 沒有 push 任何東西。

### 一個要記下來的意外

這一輪中途，session 外把 `fix/sandbox-term-passthrough` 推上去合進 `dev` 了
（`origin/dev` 從 `3d9f249` 前進到 `f96053d`，內容同一份、hash 不同 —— rebase 合併）。
本機 `dev` 在那之後被 `git pull` 帶到 `f96053d`，而當時 session 以為自己還在
`fix/` 分支上，於是 CHANGELOG 的 commit 直接落在 `dev`。

已修正：commit 移到 `docs/changelog-v021`，`dev` 用 `git branch -f` 退回 `origin/dev`，
沒有東西遺失。本機還留著 `fix/sandbox-term-passthrough`（`65c23f7`），
內容與 `dev` 上的 `f96053d` 逐字相同（`git diff` 為空），已可刪。

**形態**：長流程中途，工作樹被 session 外的操作移動，而流程自己不重新確認當前分支。
