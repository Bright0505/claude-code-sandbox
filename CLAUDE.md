# 開發規範（執行版）

給 Claude 的常駐規則。**本檔只放兩種東西**：無法被觸發、隨時適用的邊界，
以及「什麼時候載入哪個 skill」。有明確時機的做法一律在 skill 裡，本檔不重述。

人看的版本在 `ONBOARDING.md`。

**規範優先順序**（衝突時由下往上讓）：

```
1. 使用者全域 ~/.claude/CLAUDE.md      最高
2. 專案自己的 CLAUDE.md
3. 本檔（通用開發規範）
```

已在上層規定的（commit 訊息格式、署名規則、回應語言）**本檔不重述**。

---

## 什麼時候載入哪個 skill

```
收到委託 ──▶ plan ──▶ code ──▶ verify ──▶ deliver
                        ▲          │
                        └──────────┘  沒過就回去改
              任何一步：異常/可疑 ──▶ traps
              任何一步：值得記的問題或決策 ──▶ record
```

| 觸發條件 | 載入 |
|---|---|
| 收到任何開發委託，或要判斷這件事該用多重的流程 | `plan` |
| 計畫確定後，要動手改既有程式碼 | `code` |
| 改完了，要說「驗過了」之前 | `verify` |
| 驗過了，要 commit／開 PR／動 submodule | `deliver` |
| 開工前查這塊的歷史，或發現值得記的問題／決策 | `record` |
| 工具行為反直覺，或「看起來成功」但心裡不踏實 | `traps` |

**不確定要不要載入時就載入。** 漏載的代價（重踩已知的坑）遠高於多讀一份。

---

## 鐵則

無法被觸發 —— 你不會在推論時想到要去載入某個 skill，所以它們必須常駐。

1. **一手來源優於二手** —— 文件／註解／他人或其他 agent 的報告描述的是*意圖*；
   跑起來的系統才是*事實*
2. **先量測不要先推論** —— 能一行指令確認的事不要寫「應該」
3. **Reproduce before fix** —— 復現不出來就寫「無法復現」，不要假設症狀存在
4. **沒紅過的測試不算測試**
5. **不編造原因** —— 「無法從現有證據判定」比一個合理但沒查證的根因好
6. **範圍雙向紀律** —— 不擅自擴大，也不靜默縮減。任何上限／抽樣／跳過都要說
7. **可交接性是產出的一部分** —— 假設你隨時會被中斷。任何人必須能只憑 repo
   重建「現在到哪、為什麼這樣決定、下一步是什麼」

---

## 禁令

1. 不在 `main`／主幹上 commit —— 先開分支
2. 不自動 push
3. 不 force push 到共用分支
4. 不 commit 機密（`.env` 及其備份、金鑰、密碼、真實個資）
5. 不改上游／第三方套件的程式碼
6. 一個 commit 不混不同性質的改動
7. 不提交自己無法解釋或未驗證的程式碼
8. 不刪既有紀錄 —— `已知未修`／`已推翻`／被推翻的判定都要保留並標明理由
9. 不把「應該可以」當驗證結果
10. 不擅自擴大或縮小交辦範圍

---

## 回報格式

每次都用，不論強度。詳細度可以縮，**不能到零**。

```
改了什麼   逐檔列出：路徑 + 這一處做了什麼
為什麼     意圖層的理由。不可以是把 diff 重述一遍
怎麼驗的   實際跑了什麼指令、實際看到什麼輸出
沒做什麼   跳過的、假設的、有上限的、範圍外但發現的
```

- **L0／L1** —— 對話只留 `怎麼驗的`＋`沒做什麼`，其餘由 commit 訊息承擔
- **L2 以上** —— 四段完整，主體寫進 `docs/tasks/`，對話只放結論與指標

`怎麼驗的`／`沒做什麼` **不可省** —— 前者擋住「宣稱驗過但沒驗」，
後者的內容 diff 和 git log 都沒有。

---

## 專案落地時要建立的檔案

`plan` / `record` 依賴它們：

```
docs/tasks/README.md            任務索引表
docs/tasks/YYYY-MM-DD-*.md      各任務的計畫檔
docs/KNOWN-ISSUES.md            事故紀錄（可 grep 回查）
docs/DECISIONS.md               決議紀錄
```
