# 開發規範（執行版）

給 Claude 的常駐規則。人看的版本在 `ONBOARDING.md`（含名詞解釋與為什麼）。

**規範優先順序**（衝突時由下往上讓）：

```
1. 使用者全域 ~/.claude/CLAUDE.md      最高
2. 專案自己的 CLAUDE.md
3. 本檔（通用開發規範）
```

已在上層規定的（commit 訊息格式、署名規則、回應語言）**本檔不重述**，避免兩份來源漂移。

---

## 動手前四步

不管指令多短、誰下的，寫程式碼前先做完這四件（展開見 `plan`）：

1. **Scope definition** —— 講清楚範圍，**必須包含 out of scope**
2. **Locate** —— 附證據找到目標，不是猜的；含 prior art search
3. **講計畫** —— effort sizing、變更清單、怎麼驗證、怎麼回退
4. **Requirement gap triage** —— blocking 的問（最多 3 個，超過表示需求不足），
   non-blocking 自己決並寫成 assumptions

---

## 可直接做 vs 先等同意

**以下五條全部成立** → 直接做（做完仍須回報）：

1. 在交辦範圍內 —— 不是順手發現的
2. 除委託明確要求的變化外，**對外行為零變化**
3. 不動介面、依賴、權限、機密、資料
4. 能立刻驗證
5. **atomic revert** —— 單一 commit 可還原，無外部副作用

**任一條不成立** → 先講計畫等同意。L2 以上的計畫要落地成 `docs/tasks/<任務名>.md`，
不只是對話。

### 範圍外發現的問題

**一律不修。** 記錄 + 在回報裡說。判準是它會不會擋住這次的 Definition of Done
（見 `code` §5）。若它擋住驗收無法進行，**停下來說明並等指示** ——
「有東西擋住驗證」本身就是需要人決策的資訊。

---

## 回報格式（每次都用，不論強度）

```
改了什麼   逐檔列出：路徑 + 這一處做了什麼
為什麼     意圖層的理由。不可以是把 diff 重述一遍
怎麼驗的   實際跑了什麼指令、實際看到什麼輸出
沒做什麼   跳過的、假設的、有上限的、範圍外但發現的
```

詳細度**對應強度**，可以縮，**不能到零**。改一個錯字也要有一行說明。

- **L0／L1** —— 對話只留 `怎麼驗的`＋`沒做什麼`；`改了什麼`／`為什麼` 由 commit 訊息承擔
- **L2 以上** —— 四段完整，但主體寫進 `docs/tasks/`，對話只放結論與指標

`怎麼驗的`／`沒做什麼` **不可省** —— 前者擋住「宣稱驗過但沒驗」，
後者的內容 diff 和 git log 都沒有。

---

## 鐵則

1. **一手來源優於二手** —— 設定檔／文件／註解／他人或其他 agent 的報告描述的是*意圖*；
   跑起來的系統才是*事實*
2. **先量測不要先推論** —— 能一行指令確認的事不要寫「應該」。
   用 proxy metric 時先做 calibration（見 `traps` §6）
3. **Reproduce before fix** —— 記下改動前的實際輸出。復現不出來就寫「無法復現」，
   不要假設它存在
4. **沒紅過的測試不算測試** —— 新測試要經過 mutation 確認轉紅（見 `verify` §2）
5. **不編造原因** —— 寫「無法從現有證據判定」，比編一個合理的根因好
6. **範圍雙向紀律** —— 不擅自擴大，也不靜默縮減。任何上限／抽樣／跳過都要在回報裡說
7. **可交接性是產出的一部分** —— 假設你隨時會被中斷。任何人必須能只憑 repo
   重建「現在到哪、為什麼這樣決定、下一步是什麼」

---

## 禁令

1. 不在 `main`／主幹上 commit —— 先開分支
2. 不自動 push
3. 不 force push 到共用分支
4. 不 commit 機密（`.env` 及其備份、金鑰、密碼、真實個資）
5. 不改上游／第三方套件的程式碼
6. 一個 commit 不混不同性質的改動
7. 不提交自己無法解釋或未驗證的程式碼
8. 不刪既有紀錄 —— `已知未修`／`已推翻`／被推翻的判定都要保留並標明理由
9. 不把「應該可以」當驗證結果
10. 不擅自擴大或縮小交辦範圍

---

## 兩種任務主線

| | 擴充／修改既有 | 新建 |
|---|---|---|
| 動手前多一步 | **Reproduce baseline** 並留下輸出 | **Requirement gap triage** |
| 技術選擇 | 沿用既有 convention，不引進個人偏好 | **必須在計畫階段講出來並取得同意** |
| 中途 | 每步確認未破壞既有行為 | 最小可跑骨架先給人看，再長內容 |
| 驗證重點 | **Regression**：原本能做的還能做 | 對照需求逐條驗收 |
| 開工前 | **Prior art search**：這一塊以前踩過什麼 | 記下需求釐清的結論 |

---

## Effort sizing

| 強度 | 情況 | 要做什麼 |
|---|---|---|
| **L0** | 錯字、註解、格式 | 直接改 + 回報 |
| **L1** | 單一模組的 bug fix、小功能 | reproduce → 改 → 補測試（含 mutation）→ 回報 |
| **L2** | 跨檔／跨模組、牽動 3+ 檔 | L1 + 計畫檔（含完成紀錄與問題紀錄）+ pilot 一處再推廣 |
| **L3** | 架構改動、影響全部模組或使用者可見行為 | L2 + 分 phase（每個 phase 一份 `.md`）+ 三本帳 + 稽核先行 |

不確定時往高一級走，**但要說出來**。把 L1 的事套上 L3 流程是浪費。

---

## 文件規範

寫 skill、規範、計畫檔時：

1. **術語必須指向它的既定含義** —— 下筆前確認 ①這個詞原本指什麼 ②它替掉了哪一段解釋。
   替不掉就寫中文，更誠實。自檢訊號：**用了術語卻還要補幾條說明 = 用錯了**（K-1）
2. **skill 保持中性** —— 說「該做什麼」，不放實例。
   案例的去處是 `docs/KNOWN-ISSUES.md`，那裡說「這裡踩過什麼」
3. **checklist 要有增值** —— 跨小節或補充正文未講的才留，純複述一律刪（K-2）

---

## 觸發表

遇到左邊的情況，載入右邊的 skill 取得展開的做法。

| 遇到什麼 | skill |
|---|---|
| 要規劃任務、effort sizing、建計畫檔、分 phase | `plan` |
| 要記錄問題／決策，或開工前 prior art search | `record` |
| 要動手寫程式碼 | `code` |
| 要宣稱「驗過了」 | `verify` |
| 要 commit、開 PR、處理 submodule | `deliver` |
| 工具行為很怪、或「看起來成功」但可疑 | `traps` |

**專案落地時要建立的檔案**（`plan` / `record` 依賴它們）：

```
docs/tasks/README.md            任務索引表（日期／任務／涉及範圍／一句話）
docs/tasks/YYYY-MM-DD-*.md      各任務的計畫檔
docs/KNOWN-ISSUES.md            事故紀錄（可 grep 回查）
docs/DECISIONS.md               決議紀錄
```
