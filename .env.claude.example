# 複製成 .env.claude 後填值。整份檔案會被帶進容器（docker-compose.claude.yml 的
# env_file），且 .env.claude 已在 .gitignore 內，不會被提交。
# 每一項都是選用的：整個檔案不存在時 sandbox 也能正常啟動。

# 用 Claude 訂閱帳號登入的話不需要這行（見 README「快速開始」）
ANTHROPIC_API_KEY=sk-ant-xxxxxxxx


# ---- git 認證：要在容器內 commit / push 才需要 ----
# 容器內只放行 80/443，port 22 是關的，所以一律走 HTTPS + token（不是 SSH key）。
# token 只留在容器的環境變數裡，不會寫進任何檔案（見 scripts/git-credential-env.sh）。

# GitHub PAT — classic 要 repo scope；fine-grained 要 Contents: Read and write
GITHUB_TOKEN=ghp_xxxxxxxx

# GitLab PAT — 要 write_repository；另外要用 glab 開 MR／讀 issue 再加 api
GITLAB_TOKEN=glpat-xxxxxxxx

# commit 需要身分。兩個都設才生效，只設一個會被忽略並警告
GIT_USER_NAME=Your Name
GIT_USER_EMAIL=you@example.com


# ---- 以下皆為選用，預設值夠用時整段可以不管 ----

# 自架站台。可填 host 或完整 URL，兩種都會被正規化；填了會自動加進防火牆白名單
#GITLAB_HOST=gitlab.example.com
#GITHUB_HOST=github.example.com

# HTTPS 認證帶的 username。預設 GitHub=x-access-token、GitLab=oauth2
#GITHUB_USER=
#GITLAB_USER=

# 額外要放行的網域，逗號或空白分隔（不必為此修改 scripts/init-firewall.sh）
#EXTRA_ALLOWED_DOMAINS=registry.example.com,proxy.example.com

# 預設會把 git@host: 形式的 remote 改寫成 https://host/（因為容器內連不到 port 22）。
# 設 0 可關掉這個改寫
#SANDBOX_GIT_REWRITE_SSH=0

# glab 預設會回傳使用遙測給 GitLab，設 0 關閉
#GLAB_SEND_TELEMETRY=0
