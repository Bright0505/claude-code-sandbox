# 開發規範 — 新人手冊

讀一次就好，之後當字典用。

這份給**人**看。`CLAUDE.md` 是給 Claude Code 看的同一套規範的執行版本 ——
你不需要背它，但值得知道它存在，因為 Claude 的行為是照那份走的。

---

## 1. 這裡的開發長什麼樣

```
你委託  →  Claude 確認理解、提計畫  →  執行  →  回報  →  你驗收
```

**你的角色是委託者與驗收者，不是打字員。** 這代表兩件事：

- 你不需要先會寫全部的程式碼
- 但你**必須看得懂交出來的東西**，否則你沒有在驗收，只是在按同意

第二點是這份手冊存在的主要理由。

---

## 2. 你唯一要學會的事：怎麼下委託

一句「幫我改一下登入」會得到你不想要的東西 —— 不是因為 Claude 笨，而是因為
那句話有五種解讀。

**四個欄位，寫多寫少都行，但四個都要有：**

| 欄位 | 意思 | 寫不出來代表 |
|---|---|---|
| **目標** | 要達成什麼（不是要改哪個檔案） | 你在描述手段而不是需求 |
| **範圍** | 包含什麼、**明確不包含**什麼 | 範圍會在過程中悄悄膨脹 |
| **驗收** | 怎麼算做好了 | **需求還沒想清楚** |
| **參考** | 需求文件、類似的既有功能、相關紀錄 | Claude 得從零猜你的慣例 |

**「驗收」寫不出來是最重要的訊號。** 那通常表示你自己還不確定要什麼。
這時候先跟 Claude 討論需求，不要直接叫它動手。

### 好壞對照

❌ **不好**

> 幫我改一下訂單查詢，太慢了

問題：慢是多慢？可接受是多快？可以改 DB 嗎？可以改回傳的內容嗎？

✅ **好**

> **目標**：訂單查詢從 8 秒降到 2 秒以內
> **範圍**：只動查詢邏輯與索引。不改 API 回傳格式，不改前端
> **驗收**：用正式環境的資料量，同一組查詢條件跑 5 次都在 2 秒內
> **參考**：`docs/order-api.md`；商品查詢上個月做過類似優化

---

❌ **不好**

> 用 TS 幫我做一個 API

✅ **好**

> **目標**：照 `docs/requirements.md` 建一個會員資料的 REST API
> **範圍**：只做 CRUD 四個端點。不做權限、不做前端
> **驗收**：四個端點都有測試，`docs/requirements.md` 的每一條都對得上
> **參考**：`docs/requirements.md` 是需求文件。技術用 TypeScript

---

## 3. Claude 動手前會做四件事

看到它在做這些不要覺得它在拖時間 —— 這四步是為了避免做完才發現方向錯：

1. **回述** —— 「我理解你要的是 X，範圍包含 A、B，不包含 C」
   → **你要做的**：如果它理解錯了，現在就講。這時候糾正最便宜
2. **定位** —— 實際找出要改的東西在哪，列出檔案（不是猜）
3. **講計畫** —— 要改哪些地方、怎麼驗證、萬一錯了怎麼還原
4. **點出需求沒講清楚的地方**

### 關於第 4 步：它會問你，也會自己假設

需求文件永遠有沒寫到的細節，這很正常。Claude 會分成兩類處理：

**會問你的**（最多 3 個）—— 這些答錯了會白做：

> 這個 API 是給外部客戶用還是內部系統用？（決定要不要驗證身份）

**不問、但會明白告訴你它假設了什麼** —— 這些選錯了改起來便宜：

> 假設（若不對請說）
> - 時間欄位用 ISO 8601 UTC，因為需求沒指定時區
> - 分頁用 offset/limit，因為沒提到游標需求

**你不需要逐條回覆假設清單**，掃過去，只糾正你在意的。

如果它問了超過 3 個阻斷性問題，那通常表示**需求還沒到可以開工的程度** ——
那本身就是有用的資訊，先把需求談清楚。

---

## 4. 什麼時候它會直接做，什麼時候會等你

**以下五條全部成立**，它會直接動手（做完仍然會告訴你）：

1. 在你交辦的範圍內 —— 不是它順手發現的
2. 除了你要求的變化以外，對外行為沒有任何差別
3. 沒動到介面、套件依賴、權限、金鑰、資料
4. 能立刻驗證
5. 一步就能還原

**任何一條不成立 → 它會先講計畫等你同意。**

### 它順手發現別的問題時

**它不會自己修。** 它會記錄下來並告訴你，由你決定要不要處理。

如果那個問題擋住當前任務無法驗證，它會**停下來說明**，而不是自己排除障礙 ——
因為那時候你可能想換個做法，或想先處理那件事。

---

## 5. 怎麼看回報

每次回報都是同樣四段，不管改動多小：

| 段落 | 你該檢查什麼 |
|---|---|
| **改了什麼** | 檔案清單有沒有超出你的範圍 |
| **為什麼** | **這段是你學東西的地方。**看不懂就問 |
| **怎麼驗的** | 有沒有寫出**實際跑了什麼、實際看到什麼** |
| **沒做什麼** | 跳過的、假設的、有上限的、範圍外發現的 |

### 三個警訊

看到這些要追問：

- **「應該可以」「理論上沒問題」** → 那是推論不是驗證。問「你實際跑了嗎、輸出是什麼」
- **「測試通過」但沒說跑了幾個、跑了什麼** → 數字要是實際跑出來的
- **「為什麼」只是把改動重述一遍** → 「把 A 改成 B」不是理由，「因為 A 在某情況下會…」才是

---

## 6. 你的責任：能解釋每一行

**送出前，逐段講一次這段在做什麼、為什麼要這樣。講不出來的段落就問。**

沒有任何工具能檢查這件事，只能靠你。而它是這份手冊裡最重要的一條 ——
因為程式碼出問題時，要負責的是你，不是產生它的工具。

不懂就問，問 Claude 或問人都可以。**不懂但照收，是這裡唯一不能接受的做法。**

---

## 7. 十條禁令

| # | 禁令 | 為什麼 |
|---|---|---|
| 1 | 不在 `main` 上 commit —— 先開分支 | `main` 是大家的。壞了會影響所有人，而且很難乾淨地還原 |
| 2 | 不自動 push —— 等人說可以 | push 出去別人就看得到、CI 就會跑。要不要公開由人決定 |
| 3 | 不 force push 到共用分支 | 會覆蓋掉別人的東西，且救不回來 |
| 4 | 不 commit 機密 —— `.env`、金鑰、密碼、真實個資 | **進了 git 歷史就等於洩漏了**，刪掉 commit 也一樣。貼錯了要換掉那組金鑰 |
| 5 | 不改上游／第三方套件的程式碼 | 下次更新就被蓋掉，而且沒人知道你改過 |
| 6 | 一個 commit 不混不同性質的改動 | 出事時要能只還原有問題的那一段 |
| 7 | 不提交自己看不懂或沒驗證過的程式碼 | 見上一節 |
| 8 | 不刪掉既有紀錄 —— 包含「已知未修」「已推翻」 | 刪了，下一個人會重新踩一次同樣的坑 |
| 9 | 不把「應該可以」當成驗證結果 | 見上一節的警訊 |
| 10 | 不擅自擴大或縮小交辦範圍 | 範圍是委託者的。要改就講，不要默默改 |

---

## 8. 名詞表

只列這份手冊會用到的。

| 名詞 | 意思 |
|---|---|
| **branch（分支）** | 一條獨立的開發線。在上面亂改都不影響 `main` |
| **commit** | 一次存檔，附一段說明。歷史由一連串 commit 組成 |
| **push** | 把本機的 commit 送到伺服器，其他人才看得到 |
| **MR／PR** | 請求把分支合併回 `main`，通常伴隨 review |
| **回歸（regression）** | 本來好的東西被改壞了。「回歸測試」＝確認舊功能還在 |
| **突變驗證** | 故意把程式改壞，看測試會不會變紅。**沒紅過的測試不算測試** |
| **基線（baseline）** | 動手前的現況紀錄。沒有它就無法證明「改好了」 |
| **回退（rollback）** | 把改動還原。任何改動都該事先知道怎麼還原 |
| **強度 L0–L3** | 這件事該用多重的流程。L0 最輕（改錯字），L3 最重（架構改動） |

---

## 9. 常見情況怎麼辦

**「我不知道怎麼描述需求」**
先講你想解決的**問題**，不要講解法。例如「客人抱怨查訂單很慢」比「幫我加索引」好 ——
後者已經假設了解法，而那個假設可能是錯的。

**「它問我，但我答不出來」**
說「我不知道，你建議哪個」。它會給推薦與理由。**不要隨便選一個** ——
猜錯會讓後面全部建立在錯的前提上。

**「它做出來的不是我要的」**
回到「目標／範圍／驗收」四欄，看是哪一欄當初沒寫清楚。多半是「驗收」。
這不是失敗，是需求釐清的正常過程 —— 但下一次把它寫進委託裡。

**「它說驗過了，但我不放心」**
問三句：**你實際跑了什麼指令？實際輸出是什麼？如果這裡壞掉，你的測試會不會變紅？**
第三句最有用。

**「這個改動看起來比我要求的多」**
直接問「哪些是我要求的、哪些是你自己加的」。加的部分要有理由，沒理由就要求移除。

---

## 10. 更多細節在哪

| 你想知道 | 去哪 |
|---|---|
| Claude 實際照什麼規則執行 | `CLAUDE.md` |
| 這個專案自己踩過哪些坑 | 專案的事故清單（見 `CLAUDE.md` 的觸發表） |
| 怎麼在隔離環境裡跑 Claude Code | `README.md` |
